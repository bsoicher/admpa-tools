/* global expect, describe, it  */

describe('Hello world', function () {
  it('should be 11 characters long', function () {
    var e = 'Hello world'
    expect(e).to.have.length(11);
  });
  it('should be 11 characters long', function () {
    var e = 'Hello world'
    expect(e).to.have.length(11);
  });
  it('should be 11 characters long', function () {
    var e = 'Hello world'
    expect(e).to.have.length(11);
  });
  it('should be 11 characters long', function () {
    var e = 'Hello world'
    expect(e).to.have.length(11);
  });
})

describe('Hello world 2', function () {
  it('should be 11 characters long', function () {
    expect('Hello world').to.have.length(11)
  })
  it('sgdsergsedrger')
})
